---
title: Comandos Misceláneos
description: Lista y documentación de los comandos misceláneos de La Cabra.
---

import { Badge, Aside, Tabs, TabItem, Icon } from '@astrojs/starlight/components';

Esta sección detalla todos los comandos disponibles en esta categoría.

<Aside type="caution">
  Ten en cuenta que los comandos pueden ser potentes. Asegúrate de configurar correctamente los permisos en tu servidor para evitar que usuarios no autorizados puedan usarlos.
</Aside>

## Lista de comandos

### `/cabra` <Badge text="Premium" variant="success" size="small" />

Muestra una foto aleatoria de una cabra.

**Alias:** `goat`, `randomgoat`, `randomcabra`, `lacabra` | **Permisos:** <Badge text="100" variant="note" size="small" />

<Tabs>
  <TabItem label="Opciones" icon="setting">
    *Este comando no tiene opciones.*
  </TabItem>
  <TabItem label="Ejemplo de uso" icon="rocket">
    ```bash
    # Como comando de barra (slash command):
    /cabra

    # Con el prefijo configurado:
    -cabra
    ```
  </TabItem>
</Tabs>

---

### `/ender`

Este comando se me ha colado, no lo uses porfa.

**Alias:** `holasoyender` | **Permisos:** <Badge text="100" variant="note" size="small" />

<Tabs>
  <TabItem label="Opciones" icon="setting">
    *Este comando no tiene opciones.*
  </TabItem>
  <TabItem label="Ejemplo de uso" icon="rocket">
    ```bash
    # Como comando de barra (slash command):
    /ender

    # Con el prefijo configurado:
    -ender
    ```
  </TabItem>
</Tabs>

---

### `/foca` <Badge text="Premium" variant="success" size="small" />

Muestra una foto aleatoria de una foca.

**Alias:** `randomfoca`, `seal`, `seals`, `focas`, `randomseal`, `randomseals` | **Permisos:** <Badge text="100" variant="note" size="small" />

<Tabs>
  <TabItem label="Opciones" icon="setting">
    *Este comando no tiene opciones.*
  </TabItem>
  <TabItem label="Ejemplo de uso" icon="rocket">
    ```bash
    # Como comando de barra (slash command):
    /foca

    # Con el prefijo configurado:
    -foca
    ```
  </TabItem>
</Tabs>

---

### `/gato` <Badge text="Premium" variant="success" size="small" />

Muestra una imagen de un gato aleatorio.

**Alias:** `cat`, `gatito`, `gatitoaleatorio` | **Permisos:** <Badge text="50" variant="note" size="small" />

<Tabs>
  <TabItem label="Opciones" icon="setting">
    *Este comando no tiene opciones.*
  </TabItem>
  <TabItem label="Ejemplo de uso" icon="rocket">
    ```bash
    # Como comando de barra (slash command):
    /gato

    # Con el prefijo configurado:
    -gato
    ```
  </TabItem>
</Tabs>

---

### `/michi` <Badge text="Premium" variant="success" size="small" />

Muestra una foto aleatoria de un michi, pero de los grandes.

**Alias:** `randommichi`, `bigcat`, `bigmichi`, `catbig`, `michis` | **Permisos:** <Badge text="100" variant="note" size="small" />

<Tabs>
  <TabItem label="Opciones" icon="setting">
    *Este comando no tiene opciones.*
  </TabItem>
  <TabItem label="Ejemplo de uso" icon="rocket">
    ```bash
    # Como comando de barra (slash command):
    /michi

    # Con el prefijo configurado:
    -michi
    ```
  </TabItem>
</Tabs>

---

### `/miguel`

¿Blas de quién?

**Alias:** `miguelp25` | **Permisos:** <Badge text="50" variant="note" size="small" />

<Tabs>
  <TabItem label="Opciones" icon="setting">
    *Este comando no tiene opciones.*
  </TabItem>
  <TabItem label="Ejemplo de uso" icon="rocket">
    ```bash
    # Como comando de barra (slash command):
    /miguel

    # Con el prefijo configurado:
    -miguel
    ```
  </TabItem>
</Tabs>

---

### `/nutria` <Badge text="Premium" variant="success" size="small" />

Muestra una foto aleatoria de una nutria.

**Alias:** `otter`, `randomoter`, `randomnutria`, `nutrias` | **Permisos:** <Badge text="100" variant="note" size="small" />

<Tabs>
  <TabItem label="Opciones" icon="setting">
    *Este comando no tiene opciones.*
  </TabItem>
  <TabItem label="Ejemplo de uso" icon="rocket">
    ```bash
    # Como comando de barra (slash command):
    /nutria

    # Con el prefijo configurado:
    -nutria
    ```
  </TabItem>
</Tabs>

---

### `/panda` <Badge text="Premium" variant="success" size="small" />

Muestra una foto aleatoria de un panda.

**Alias:** `randompanda`, `pandas`, `randompandas` | **Permisos:** <Badge text="100" variant="note" size="small" />

<Tabs>
  <TabItem label="Opciones" icon="setting">
    *Este comando no tiene opciones.*
  </TabItem>
  <TabItem label="Ejemplo de uso" icon="rocket">
    ```bash
    # Como comando de barra (slash command):
    /panda

    # Con el prefijo configurado:
    -panda
    ```
  </TabItem>
</Tabs>

---

### `/pato` <Badge text="Premium" variant="success" size="small" />

Muestra una imagen de un pato aleatorio.

**Alias:** `duck`, `randomduck` | **Permisos:** <Badge text="50" variant="note" size="small" />

<Tabs>
  <TabItem label="Opciones" icon="setting">
    *Este comando no tiene opciones.*
  </TabItem>
  <TabItem label="Ejemplo de uso" icon="rocket">
    ```bash
    # Como comando de barra (slash command):
    /pato

    # Con el prefijo configurado:
    -pato
    ```
  </TabItem>
</Tabs>

---

### `/paul`

Paul, simplemente.

**Alias:** `pol` | **Permisos:** <Badge text="50" variant="note" size="small" />

<Tabs>
  <TabItem label="Opciones" icon="setting">
    *Este comando no tiene opciones.*
  </TabItem>
  <TabItem label="Ejemplo de uso" icon="rocket">
    ```bash
    # Como comando de barra (slash command):
    /paul

    # Con el prefijo configurado:
    -paul
    ```
  </TabItem>
</Tabs>

---

### `/perro` <Badge text="Premium" variant="success" size="small" />

Muestra una imagen de un perro aleatorio.

**Alias:** `dog`, `randomdog` | **Permisos:** <Badge text="50" variant="note" size="small" />

<Tabs>
  <TabItem label="Opciones" icon="setting">
    *Este comando no tiene opciones.*
  </TabItem>
  <TabItem label="Ejemplo de uso" icon="rocket">
    ```bash
    # Como comando de barra (slash command):
    /perro

    # Con el prefijo configurado:
    -perro
    ```
  </TabItem>
</Tabs>

---

### `/tirar`

Devuelve un número aleatorio entre 1 y el número provisto como argumento.

**Permisos:** <Badge text="50" variant="note" size="small" />

<Tabs>
  <TabItem label="Opciones" icon="setting">
    - **`número`** <Badge text="NÚMERO" variant="default" size="small" /> <Badge text="Requerido" variant="danger" size="small" />: El número máximo
  </TabItem>
  <TabItem label="Ejemplo de uso" icon="rocket">
    ```bash
    # Como comando de barra (slash command):
    /tirar número:texto_ejemplo

    # Con el prefijo configurado:
    -tirar texto_ejemplo
    ```
  </TabItem>
</Tabs>

---

### `/web`

Comprobar si una web es segura o no.

**Alias:** `url`, `webcheck`, `urlcheck` | **Permisos:** <Badge text="50" variant="note" size="small" />

<Tabs>
  <TabItem label="Opciones" icon="setting">
    - **`url`** <Badge text="TEXTO" variant="default" size="small" /> <Badge text="Requerido" variant="danger" size="small" />: URL de la web a comprobar
  </TabItem>
  <TabItem label="Ejemplo de uso" icon="rocket">
    ```bash
    # Como comando de barra (slash command):
    /web url:texto_ejemplo

    # Con el prefijo configurado:
    -web texto_ejemplo
    ```
  </TabItem>
</Tabs>

---

### `/zorro` <Badge text="Premium" variant="success" size="small" />

Muestra una imagen de un zorro aleatorio.

**Alias:** `fox`, `randomfox` | **Permisos:** <Badge text="50" variant="note" size="small" />

<Tabs>
  <TabItem label="Opciones" icon="setting">
    *Este comando no tiene opciones.*
  </TabItem>
  <TabItem label="Ejemplo de uso" icon="rocket">
    ```bash
    # Como comando de barra (slash command):
    /zorro

    # Con el prefijo configurado:
    -zorro
    ```
  </TabItem>
</Tabs>

---

